---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: home-assistant
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: home-assistant
      version: 13.4.2
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: homeassistant/home-assistant
      tag: 2022.12.3
    env:
      TZ: "Pacific/Auckland"
    hostNetwork: true
    dnsPolicy: ClusterFirstWithHostNet
    service:
      main:
        ports:
          http:
            port: 8123
    ingress:
      main:
        enabled: true
        primary: true
        nameOverride:
        annotations: 
          traefik.ingress.kubernetes.io/router.entrypoints: web
        ingressClassName: "traefik"
        hosts:
          host: hass.rupamarpan.192.168.50.115.sslip.io
          ## Configure the paths for the host
          paths:
          - path: /
            pathType: Prefix
