apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hass-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  persistentVolumeReclaimPolicy: "Retain"
  resources:
    requests:
      storage: 2Gi